{
    "sqltools.connections": [
        {
            "previewLimit": 50,
            "driver": "SQLite",
            "name": "local",
            "group": "local",
            "database": "${workspaceFolder:demo}/db/light.db"
        }
    ]
}